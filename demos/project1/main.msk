
import apa;

export func main(std::string_view[] args)->void {
    let apa = Apa{
        .x = 0,
        .y = 1,
    };

    apa.move(10, 20);

    print("hello there! {}, {}\n", apa.x, apa.y);

    return 0;
}
